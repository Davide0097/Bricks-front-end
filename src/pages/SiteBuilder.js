import React from 'react'

// Component containing the editor and the preview
import EditorAndPreview from '../builder-components/EditorAndPreview'

const SiteBuilder = () => {
  return (
    <div className="pt-[300px] w-full h-screen">
          <EditorAndPreview />
    </div>
  
  )
}

export default SiteBuilder;
 


